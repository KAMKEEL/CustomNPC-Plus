package noppes.npcs.attribute;

import noppes.npcs.attribute.player.PlayerAttributeTracker;

import java.util.Random;

public class DamageCalculator {
    private static final Random random = new Random();


    public static double calculateDamage(PlayerAttributeTracker attacker, PlayerAttributeTracker defender, int baseDamage) {
       return 0;
    }

}
