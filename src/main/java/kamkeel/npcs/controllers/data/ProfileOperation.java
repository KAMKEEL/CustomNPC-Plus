package kamkeel.npcs.controllers.data;

public enum ProfileOperation {
    SUCCESS,
    ERROR,
    LOCKED,
    PLAYER_NOT_FOUND,
    NEW_SLOT_CREATED,
    ALREADY_ACTIVE,
    VERIFICATION_FAILED,
    MAX_SLOTS
}

