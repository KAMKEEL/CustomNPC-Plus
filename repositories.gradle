// Add any additional repositories for your dependencies here.

repositories {
    maven {
        name = "sponge"
        url = "https://repo.spongepowered.org/maven/"
    }
    repositories {
        flatDir {
            dirs 'lib'
        }
    }
}
